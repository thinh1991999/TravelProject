import React from "react";

const Destinations = ({ size }: { size?: string }) => {
  return (
    <div
      style={{
        width: size,
      }}
    >
      Destinations
    </div>
  );
};

export default Destinations;
